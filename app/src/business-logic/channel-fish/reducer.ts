import { Reduce } from '@actyx/pond';
import { ChannelFishState } from './types';

export const reducer: Reduce<ChannelFishState, {}> = (
  state,
  event,
  meta
): ChannelFishState => {
  return state;
};
